"""Service IA - resume hebdomadaire famille contextualise.

IA1:
- Contexte: planning semaine, inventaire, budget, score Jules, meteo.
- Utilisable par endpoint dashboard et cron resume_hebdo.
"""

from __future__ import annotations

from datetime import date, timedelta
from typing import Any

from pydantic import BaseModel, Field
from sqlalchemy import func

from src.core.ai import obtenir_client_ia
from src.core.db import obtenir_contexte_db
from src.services.core.base import BaseAIService
from src.services.core.registry import service_factory
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


class ResumeHebdoIAResponse(BaseModel):
    """Structure de sortie IA pour le resume hebdo."""

    resume: str = ""
    points_attention: list[str] = Field(default_factory=list)
    recommandations: list[str] = Field(default_factory=list)
    score_semaine: int = Field(default=0, ge=0, le=100)


class ResumeFamilleIAService(BaseAIService):
    """Genere un resume hebdomadaire famille avec contexte multi-modules."""

    def __init__(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁResumeFamilleIAServiceǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁResumeFamilleIAServiceǁ__init____mutmut_mutants'), args, kwargs, self)

    def xǁResumeFamilleIAServiceǁ__init____mutmut_orig(self) -> None:
        super().__init__(
            client=obtenir_client_ia(),
            cache_prefix="dashboard_resume_hebdo_ia",
            default_ttl=1800,
            service_name="resume_famille_ia",
        )

    def xǁResumeFamilleIAServiceǁ__init____mutmut_1(self) -> None:
        super().__init__(
            client=None,
            cache_prefix="dashboard_resume_hebdo_ia",
            default_ttl=1800,
            service_name="resume_famille_ia",
        )

    def xǁResumeFamilleIAServiceǁ__init____mutmut_2(self) -> None:
        super().__init__(
            client=obtenir_client_ia(),
            cache_prefix=None,
            default_ttl=1800,
            service_name="resume_famille_ia",
        )

    def xǁResumeFamilleIAServiceǁ__init____mutmut_3(self) -> None:
        super().__init__(
            client=obtenir_client_ia(),
            cache_prefix="dashboard_resume_hebdo_ia",
            default_ttl=None,
            service_name="resume_famille_ia",
        )

    def xǁResumeFamilleIAServiceǁ__init____mutmut_4(self) -> None:
        super().__init__(
            client=obtenir_client_ia(),
            cache_prefix="dashboard_resume_hebdo_ia",
            default_ttl=1800,
            service_name=None,
        )

    def xǁResumeFamilleIAServiceǁ__init____mutmut_5(self) -> None:
        super().__init__(
            cache_prefix="dashboard_resume_hebdo_ia",
            default_ttl=1800,
            service_name="resume_famille_ia",
        )

    def xǁResumeFamilleIAServiceǁ__init____mutmut_6(self) -> None:
        super().__init__(
            client=obtenir_client_ia(),
            default_ttl=1800,
            service_name="resume_famille_ia",
        )

    def xǁResumeFamilleIAServiceǁ__init____mutmut_7(self) -> None:
        super().__init__(
            client=obtenir_client_ia(),
            cache_prefix="dashboard_resume_hebdo_ia",
            service_name="resume_famille_ia",
        )

    def xǁResumeFamilleIAServiceǁ__init____mutmut_8(self) -> None:
        super().__init__(
            client=obtenir_client_ia(),
            cache_prefix="dashboard_resume_hebdo_ia",
            default_ttl=1800,
            )

    def xǁResumeFamilleIAServiceǁ__init____mutmut_9(self) -> None:
        super().__init__(
            client=obtenir_client_ia(),
            cache_prefix="XXdashboard_resume_hebdo_iaXX",
            default_ttl=1800,
            service_name="resume_famille_ia",
        )

    def xǁResumeFamilleIAServiceǁ__init____mutmut_10(self) -> None:
        super().__init__(
            client=obtenir_client_ia(),
            cache_prefix="DASHBOARD_RESUME_HEBDO_IA",
            default_ttl=1800,
            service_name="resume_famille_ia",
        )

    def xǁResumeFamilleIAServiceǁ__init____mutmut_11(self) -> None:
        super().__init__(
            client=obtenir_client_ia(),
            cache_prefix="dashboard_resume_hebdo_ia",
            default_ttl=1801,
            service_name="resume_famille_ia",
        )

    def xǁResumeFamilleIAServiceǁ__init____mutmut_12(self) -> None:
        super().__init__(
            client=obtenir_client_ia(),
            cache_prefix="dashboard_resume_hebdo_ia",
            default_ttl=1800,
            service_name="XXresume_famille_iaXX",
        )

    def xǁResumeFamilleIAServiceǁ__init____mutmut_13(self) -> None:
        super().__init__(
            client=obtenir_client_ia(),
            cache_prefix="dashboard_resume_hebdo_ia",
            default_ttl=1800,
            service_name="RESUME_FAMILLE_IA",
        )
    
    xǁResumeFamilleIAServiceǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁResumeFamilleIAServiceǁ__init____mutmut_1': xǁResumeFamilleIAServiceǁ__init____mutmut_1, 
        'xǁResumeFamilleIAServiceǁ__init____mutmut_2': xǁResumeFamilleIAServiceǁ__init____mutmut_2, 
        'xǁResumeFamilleIAServiceǁ__init____mutmut_3': xǁResumeFamilleIAServiceǁ__init____mutmut_3, 
        'xǁResumeFamilleIAServiceǁ__init____mutmut_4': xǁResumeFamilleIAServiceǁ__init____mutmut_4, 
        'xǁResumeFamilleIAServiceǁ__init____mutmut_5': xǁResumeFamilleIAServiceǁ__init____mutmut_5, 
        'xǁResumeFamilleIAServiceǁ__init____mutmut_6': xǁResumeFamilleIAServiceǁ__init____mutmut_6, 
        'xǁResumeFamilleIAServiceǁ__init____mutmut_7': xǁResumeFamilleIAServiceǁ__init____mutmut_7, 
        'xǁResumeFamilleIAServiceǁ__init____mutmut_8': xǁResumeFamilleIAServiceǁ__init____mutmut_8, 
        'xǁResumeFamilleIAServiceǁ__init____mutmut_9': xǁResumeFamilleIAServiceǁ__init____mutmut_9, 
        'xǁResumeFamilleIAServiceǁ__init____mutmut_10': xǁResumeFamilleIAServiceǁ__init____mutmut_10, 
        'xǁResumeFamilleIAServiceǁ__init____mutmut_11': xǁResumeFamilleIAServiceǁ__init____mutmut_11, 
        'xǁResumeFamilleIAServiceǁ__init____mutmut_12': xǁResumeFamilleIAServiceǁ__init____mutmut_12, 
        'xǁResumeFamilleIAServiceǁ__init____mutmut_13': xǁResumeFamilleIAServiceǁ__init____mutmut_13
    }
    xǁResumeFamilleIAServiceǁ__init____mutmut_orig.__name__ = 'xǁResumeFamilleIAServiceǁ__init__'

    def _collecter_contexte(self, reference: date | None = None) -> dict[str, Any]:
        args = [reference]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_orig'), object.__getattribute__(self, 'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_mutants'), args, kwargs, self)

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_orig(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_1(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = None
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_2(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference and date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_3(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = None
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_4(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today + timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_5(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=None)
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_6(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = None
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_7(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine - timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_8(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=None)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_9(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=7)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_10(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = None

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_11(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today - timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_12(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=None)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_13(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=8)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_14(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = None

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_15(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "XXperiodeXX": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_16(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "PERIODE": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_17(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "XXdebutXX": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_18(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "DEBUT": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_19(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "XXfinXX": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_20(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "FIN": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_21(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "XXplanningXX": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_22(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "PLANNING": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_23(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"XXrepas_planifies_7jXX": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_24(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"REPAS_PLANIFIES_7J": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_25(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 1, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_26(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "XXactivites_7jXX": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_27(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "ACTIVITES_7J": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_28(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 1, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_29(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "XXevenements_7jXX": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_30(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "EVENEMENTS_7J": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_31(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 1},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_32(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "XXinventaireXX": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_33(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "INVENTAIRE": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_34(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"XXstock_basXX": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_35(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"STOCK_BAS": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_36(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 1, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_37(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "XXperemptions_7jXX": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_38(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "PEREMPTIONS_7J": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_39(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 1},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_40(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "XXbudgetXX": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_41(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "BUDGET": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_42(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"XXdepenses_moisXX": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_43(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"DEPENSES_MOIS": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_44(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 1.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_45(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "XXscore_julesXX": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_46(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "SCORE_JULES": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_47(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"XXtotal_pointsXX": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_48(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"TOTAL_POINTS": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_49(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 1},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_50(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "XXmeteoXX": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_51(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "METEO": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_52(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"XXconditionsXX": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_53(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"CONDITIONS": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_54(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "XXtendanceXX": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_55(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "TENDANCE": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_56(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "XXinconnueXX"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_57(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "INCONNUE"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_58(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = None
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_59(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar() and 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_60(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(None, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_61(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, None)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_62(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_63(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, )
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_64(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(None)
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_65(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(None))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_66(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas > today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_67(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas < fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_68(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 1
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_69(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = None
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_70(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar() and 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_71(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    None,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_72(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    None,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_73(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_74(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_75(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(None)
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_76(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(None))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_77(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue > today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_78(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue < fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_79(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 1
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_80(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = None

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_81(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar() and 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_82(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    None,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_83(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    None,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_84(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_85(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_86(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(None)
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_87(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(None))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_88(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut > today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_89(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut < fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_90(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 1
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_91(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = None
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_92(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar() and 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_93(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(None)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_94(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(None)
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_95(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(None))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_96(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite <= ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_97(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 1
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_98(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = None

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_99(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar() and 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_100(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    None,
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_101(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    None,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_102(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    None,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_103(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_104(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_105(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_106(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(None)
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_107(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(None))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_108(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption > today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_109(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption < fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_110(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 1
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_111(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = None
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_112(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=None)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_113(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=2)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_114(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = None

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_115(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar() and 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_116(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(None)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_117(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(None)
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_118(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(None))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_119(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date > debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_120(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 1.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_121(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = None
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_122(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["XXplanningXX"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_123(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["PLANNING"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_124(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "XXrepas_planifies_7jXX": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_125(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "REPAS_PLANIFIES_7J": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_126(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(None),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_127(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "XXactivites_7jXX": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_128(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "ACTIVITES_7J": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_129(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(None),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_130(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "XXevenements_7jXX": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_131(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "EVENEMENTS_7J": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_132(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(None),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_133(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = None
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_134(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["XXinventaireXX"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_135(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["INVENTAIRE"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_136(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "XXstock_basXX": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_137(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "STOCK_BAS": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_138(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(None),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_139(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "XXperemptions_7jXX": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_140(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "PEREMPTIONS_7J": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_141(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(None),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_142(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = None

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_143(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["XXbudgetXX"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_144(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["BUDGET"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_145(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"XXdepenses_moisXX": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_146(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"DEPENSES_MOIS": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_147(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(None)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_148(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = None
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_149(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() and {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_150(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = None
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_151(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["XXscore_julesXX"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_152(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["SCORE_JULES"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_153(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "XXtotal_pointsXX": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_154(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "TOTAL_POINTS": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_155(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(None),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_156(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get(None, 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_157(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", None)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_158(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get(0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_159(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", )),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_160(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("XXtotal_pointsXX", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_161(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("TOTAL_POINTS", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_162(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 1)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_163(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "XXbadgesXX": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_164(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "BADGES": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_165(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get(None, []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_166(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", None),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_167(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get([]),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_168(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", ),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_169(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("XXbadgesXX", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_170(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("BADGES", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_171(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = None

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_172(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["XXscore_julesXX"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_173(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["SCORE_JULES"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_174(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"XXtotal_pointsXX": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_175(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"TOTAL_POINTS": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_176(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 1, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_177(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "XXbadgesXX": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_178(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "BADGES": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_179(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = None
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_180(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=None)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_181(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=4)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_182(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = None
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_183(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "XXjourXX": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_184(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "JOUR": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_185(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(None, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_186(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, None, None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_187(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr("date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_188(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_189(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", ).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_190(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "XXdateXX", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_191(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "DATE", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_192(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(None, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_193(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, None, None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_194(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr("date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_195(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_196(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", )
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_197(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "XXdateXX", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_198(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "DATE", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_199(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "XXconditionXX": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_200(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "CONDITION": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_201(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(None, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_202(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, None, ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_203(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", None),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_204(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr("condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_205(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_206(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_207(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "XXconditionXX", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_208(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "CONDITION", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_209(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", "XXXX"),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_210(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "XXtminXX": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_211(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "TMIN": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_212(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(None),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_213(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) and 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_214(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(None, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_215(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, None, 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_216(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", None) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_217(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr("temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_218(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_219(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", ) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_220(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "XXtemperature_minXX", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_221(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "TEMPERATURE_MIN", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_222(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 1.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_223(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 1.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_224(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "XXtmaxXX": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_225(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "TMAX": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_226(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(None),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_227(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) and 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_228(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(None, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_229(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, None, 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_230(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", None) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_231(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr("temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_232(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_233(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", ) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_234(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "XXtemperature_maxXX", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_235(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "TEMPERATURE_MAX", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_236(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 1.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_237(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 1.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_238(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions and [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_239(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = None
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_240(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[1]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_241(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["XXconditionXX"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_242(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["CONDITION"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_243(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "XXinconnueXX"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_244(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "INCONNUE"
            contexte["meteo"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_245(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = None
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_246(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["XXmeteoXX"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_247(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["METEO"] = {"conditions": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_248(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"XXconditionsXX": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_249(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"CONDITIONS": conditions, "tendance": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_250(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "XXtendanceXX": tendance}
        except Exception:
            pass

        return contexte

    def xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_251(self, reference: date | None = None) -> dict[str, Any]:
        """Collecte le contexte metier pour l'IA."""
        today = reference or date.today()
        debut_semaine = today - timedelta(days=today.weekday())
        fin_semaine = debut_semaine + timedelta(days=6)
        fin_horizon = today + timedelta(days=7)

        contexte: dict[str, Any] = {
            "periode": {
                "debut": debut_semaine.isoformat(),
                "fin": fin_semaine.isoformat(),
            },
            "planning": {"repas_planifies_7j": 0, "activites_7j": 0, "evenements_7j": 0},
            "inventaire": {"stock_bas": 0, "peremptions_7j": 0},
            "budget": {"depenses_mois": 0.0},
            "score_jules": {"total_points": 0},
            "meteo": {"conditions": [], "tendance": "inconnue"},
        }

        with obtenir_contexte_db() as session:
            from src.core.models import ActiviteFamille, ArticleInventaire, BudgetFamille, Repas
            from src.core.models.calendrier import EvenementPlanning

            # Planning semaine/horizon court
            repas_planifies = (
                session.query(func.count(Repas.id))
                .filter(Repas.date_repas >= today, Repas.date_repas <= fin_horizon)
                .scalar()
                or 0
            )
            activites = (
                session.query(func.count(ActiviteFamille.id))
                .filter(
                    ActiviteFamille.date_prevue >= today,
                    ActiviteFamille.date_prevue <= fin_horizon,
                )
                .scalar()
                or 0
            )
            evenements = (
                session.query(func.count(EvenementPlanning.id))
                .filter(
                    EvenementPlanning.date_debut >= today,
                    EvenementPlanning.date_debut <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Inventaire
            stock_bas = (
                session.query(func.count(ArticleInventaire.id))
                .filter(ArticleInventaire.quantite < ArticleInventaire.quantite_min)
                .scalar()
                or 0
            )
            peremptions = (
                session.query(func.count(ArticleInventaire.id))
                .filter(
                    ArticleInventaire.date_peremption.isnot(None),
                    ArticleInventaire.date_peremption >= today,
                    ArticleInventaire.date_peremption <= fin_horizon,
                )
                .scalar()
                or 0
            )

            # Budget mois courant
            debut_mois = today.replace(day=1)
            depenses_mois = (
                session.query(func.sum(BudgetFamille.montant))
                .filter(BudgetFamille.date >= debut_mois)
                .scalar()
                or 0.0
            )

            contexte["planning"] = {
                "repas_planifies_7j": int(repas_planifies),
                "activites_7j": int(activites),
                "evenements_7j": int(evenements),
            }
            contexte["inventaire"] = {
                "stock_bas": int(stock_bas),
                "peremptions_7j": int(peremptions),
            }
            contexte["budget"] = {"depenses_mois": float(depenses_mois)}

        # Score Jules (gamification famille)
        try:
            from src.services.dashboard.points_famille import obtenir_points_famille_service

            points = obtenir_points_famille_service().calculer_points() or {}
            contexte["score_jules"] = {
                "total_points": int(points.get("total_points", 0)),
                "badges": points.get("badges", []),
            }
        except Exception:
            contexte["score_jules"] = {"total_points": 0, "badges": []}

        # Meteo (3 prochains jours)
        try:
            from src.services.integrations.weather import obtenir_service_meteo

            previsions = obtenir_service_meteo().get_previsions(nb_jours=3)
            conditions = [
                {
                    "jour": getattr(p, "date", None).isoformat()
                    if getattr(p, "date", None)
                    else None,
                    "condition": getattr(p, "condition", ""),
                    "tmin": float(getattr(p, "temperature_min", 0.0) or 0.0),
                    "tmax": float(getattr(p, "temperature_max", 0.0) or 0.0),
                }
                for p in (previsions or [])
            ]
            tendance = conditions[0]["condition"] if conditions else "inconnue"
            contexte["meteo"] = {"conditions": conditions, "TENDANCE": tendance}
        except Exception:
            pass

        return contexte
    
    xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_1': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_1, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_2': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_2, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_3': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_3, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_4': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_4, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_5': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_5, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_6': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_6, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_7': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_7, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_8': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_8, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_9': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_9, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_10': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_10, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_11': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_11, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_12': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_12, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_13': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_13, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_14': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_14, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_15': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_15, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_16': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_16, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_17': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_17, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_18': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_18, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_19': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_19, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_20': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_20, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_21': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_21, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_22': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_22, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_23': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_23, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_24': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_24, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_25': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_25, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_26': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_26, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_27': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_27, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_28': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_28, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_29': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_29, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_30': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_30, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_31': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_31, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_32': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_32, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_33': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_33, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_34': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_34, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_35': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_35, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_36': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_36, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_37': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_37, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_38': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_38, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_39': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_39, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_40': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_40, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_41': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_41, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_42': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_42, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_43': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_43, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_44': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_44, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_45': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_45, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_46': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_46, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_47': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_47, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_48': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_48, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_49': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_49, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_50': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_50, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_51': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_51, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_52': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_52, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_53': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_53, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_54': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_54, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_55': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_55, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_56': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_56, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_57': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_57, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_58': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_58, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_59': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_59, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_60': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_60, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_61': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_61, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_62': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_62, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_63': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_63, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_64': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_64, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_65': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_65, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_66': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_66, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_67': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_67, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_68': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_68, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_69': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_69, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_70': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_70, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_71': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_71, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_72': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_72, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_73': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_73, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_74': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_74, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_75': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_75, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_76': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_76, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_77': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_77, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_78': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_78, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_79': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_79, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_80': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_80, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_81': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_81, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_82': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_82, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_83': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_83, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_84': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_84, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_85': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_85, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_86': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_86, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_87': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_87, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_88': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_88, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_89': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_89, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_90': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_90, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_91': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_91, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_92': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_92, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_93': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_93, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_94': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_94, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_95': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_95, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_96': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_96, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_97': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_97, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_98': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_98, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_99': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_99, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_100': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_100, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_101': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_101, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_102': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_102, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_103': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_103, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_104': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_104, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_105': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_105, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_106': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_106, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_107': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_107, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_108': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_108, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_109': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_109, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_110': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_110, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_111': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_111, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_112': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_112, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_113': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_113, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_114': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_114, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_115': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_115, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_116': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_116, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_117': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_117, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_118': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_118, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_119': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_119, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_120': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_120, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_121': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_121, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_122': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_122, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_123': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_123, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_124': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_124, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_125': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_125, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_126': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_126, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_127': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_127, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_128': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_128, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_129': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_129, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_130': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_130, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_131': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_131, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_132': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_132, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_133': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_133, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_134': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_134, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_135': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_135, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_136': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_136, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_137': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_137, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_138': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_138, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_139': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_139, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_140': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_140, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_141': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_141, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_142': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_142, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_143': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_143, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_144': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_144, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_145': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_145, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_146': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_146, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_147': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_147, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_148': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_148, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_149': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_149, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_150': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_150, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_151': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_151, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_152': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_152, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_153': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_153, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_154': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_154, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_155': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_155, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_156': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_156, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_157': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_157, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_158': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_158, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_159': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_159, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_160': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_160, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_161': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_161, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_162': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_162, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_163': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_163, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_164': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_164, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_165': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_165, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_166': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_166, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_167': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_167, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_168': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_168, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_169': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_169, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_170': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_170, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_171': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_171, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_172': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_172, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_173': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_173, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_174': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_174, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_175': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_175, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_176': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_176, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_177': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_177, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_178': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_178, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_179': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_179, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_180': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_180, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_181': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_181, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_182': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_182, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_183': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_183, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_184': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_184, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_185': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_185, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_186': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_186, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_187': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_187, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_188': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_188, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_189': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_189, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_190': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_190, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_191': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_191, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_192': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_192, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_193': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_193, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_194': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_194, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_195': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_195, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_196': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_196, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_197': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_197, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_198': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_198, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_199': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_199, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_200': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_200, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_201': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_201, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_202': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_202, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_203': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_203, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_204': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_204, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_205': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_205, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_206': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_206, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_207': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_207, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_208': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_208, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_209': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_209, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_210': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_210, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_211': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_211, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_212': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_212, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_213': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_213, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_214': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_214, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_215': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_215, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_216': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_216, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_217': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_217, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_218': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_218, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_219': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_219, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_220': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_220, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_221': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_221, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_222': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_222, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_223': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_223, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_224': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_224, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_225': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_225, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_226': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_226, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_227': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_227, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_228': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_228, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_229': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_229, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_230': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_230, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_231': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_231, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_232': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_232, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_233': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_233, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_234': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_234, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_235': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_235, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_236': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_236, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_237': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_237, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_238': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_238, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_239': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_239, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_240': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_240, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_241': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_241, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_242': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_242, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_243': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_243, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_244': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_244, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_245': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_245, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_246': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_246, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_247': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_247, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_248': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_248, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_249': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_249, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_250': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_250, 
        'xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_251': xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_251
    }
    xǁResumeFamilleIAServiceǁ_collecter_contexte__mutmut_orig.__name__ = 'xǁResumeFamilleIAServiceǁ_collecter_contexte'

    def generer_resume_hebdo(self, reference: date | None = None) -> dict[str, Any]:
        args = [reference]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_orig'), object.__getattribute__(self, 'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_mutants'), args, kwargs, self)

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_orig(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_1(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = None

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_2(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(None)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_3(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = None

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_4(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "XXGenere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\nXX"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_5(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "genere un resume hebdomadaire familial concis et utile a partir du contexte json suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_6(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "GENERE UN RESUME HEBDOMADAIRE FAMILIAL CONCIS ET UTILE A PARTIR DU CONTEXTE JSON SUIVANT:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_7(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "XXRetourne uniquement du JSON avec les cles: XX"
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_8(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "retourne uniquement du json avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_9(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "RETOURNE UNIQUEMENT DU JSON AVEC LES CLES: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_10(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "XXresume, points_attention (liste), recommandations (liste), score_semaine (0-100).XX"
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_11(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "RESUME, POINTS_ATTENTION (LISTE), RECOMMANDATIONS (LISTE), SCORE_SEMAINE (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_12(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = None

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_13(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "XXTu es un coach familial bienveillant. XX"
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_14(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_15(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "TU ES UN COACH FAMILIAL BIENVEILLANT. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_16(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "XXPriorise actions concretes pour la semaine a venir. XX"
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_17(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_18(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "PRIORISE ACTIONS CONCRETES POUR LA SEMAINE A VENIR. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_19(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "XXReponds en francais avec JSON strict.XX"
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_20(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "reponds en francais avec json strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_21(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "REPONDS EN FRANCAIS AVEC JSON STRICT."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_22(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = None

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_23(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=None,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_24(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=None,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_25(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=None,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_26(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=None,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_27(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=None,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_28(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_29(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_30(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_31(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_32(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_33(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=701,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_34(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=False,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_35(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is not None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_36(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = None
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_37(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["XXinventaireXX"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_38(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["INVENTAIRE"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_39(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["XXstock_basXX"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_40(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["STOCK_BAS"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_41(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = None
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_42(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["XXinventaireXX"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_43(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["INVENTAIRE"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_44(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["XXperemptions_7jXX"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_45(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["PEREMPTIONS_7J"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_46(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = None
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_47(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["XXplanningXX"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_48(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["PLANNING"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_49(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["XXrepas_planifies_7jXX"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_50(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["REPAS_PLANIFIES_7J"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_51(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = None
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_52(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                None,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_53(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                None,
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_54(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_55(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_56(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                1,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_57(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    None,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_58(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    None,
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_59(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_60(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_61(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    101,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_62(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) - min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_63(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) + (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_64(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 + (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_65(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    81 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_66(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas / 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_67(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 4) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_68(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions / 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_69(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 3) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_70(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(None, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_71(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, None),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_72(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_73(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, ),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_74(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 15),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_75(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = None

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_76(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=None,
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_77(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=None,
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_78(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=None,
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_79(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=None,
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_80(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_81(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_82(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_83(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_84(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "XXSemaine globalement stable. XX"
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_85(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_86(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "SEMAINE GLOBALEMENT STABLE. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_87(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "XXVerifier les stocks faibles avant les coursesXX",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_88(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_89(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "VERIFIER LES STOCKS FAIBLES AVANT LES COURSES",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_90(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "XXPrioriser les produits proches de peremptionXX",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_91(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_92(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "PRIORISER LES PRODUITS PROCHES DE PEREMPTION",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_93(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "XXBloquer 20 minutes pour preparer la liste de coursesXX",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_94(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_95(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "BLOQUER 20 MINUTES POUR PREPARER LA LISTE DE COURSES",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_96(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "XXPlanifier 1 activite famille exterieure selon la meteoXX",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_97(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_98(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "PLANIFIER 1 ACTIVITE FAMILLE EXTERIEURE SELON LA METEO",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_99(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "XXRevoir le budget de fin de mois avant weekendXX",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_100(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_101(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "REVOIR LE BUDGET DE FIN DE MOIS AVANT WEEKEND",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_102(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(None),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_103(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "XXperiodeXX": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_104(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "PERIODE": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_105(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get(None),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_106(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("XXperiodeXX"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_107(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("PERIODE"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_108(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "XXcontexteXX": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_109(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "CONTEXTE": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_110(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "XXresumeXX": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_111(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "RESUME": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_112(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "XXpoints_attentionXX": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_113(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "POINTS_ATTENTION": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_114(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "XXrecommandationsXX": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_115(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "RECOMMANDATIONS": parsed.recommandations,
            "score_semaine": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_116(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "XXscore_semaineXX": parsed.score_semaine,
        }

    def xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_117(self, reference: date | None = None) -> dict[str, Any]:
        """Genere un resume hebdo contextualise."""
        contexte = self._collecter_contexte(reference)

        prompt = (
            "Genere un resume hebdomadaire familial concis et utile a partir du contexte JSON suivant:\n"
            f"{contexte}\n\n"
            "Retourne uniquement du JSON avec les cles: "
            "resume, points_attention (liste), recommandations (liste), score_semaine (0-100)."
        )

        system_prompt = (
            "Tu es un coach familial bienveillant. "
            "Priorise actions concretes pour la semaine a venir. "
            "Reponds en francais avec JSON strict."
        )

        parsed = self.call_with_json_parsing_sync(
            prompt=prompt,
            response_model=ResumeHebdoIAResponse,
            system_prompt=system_prompt,
            max_tokens=700,
            use_cache=True,
        )

        if parsed is None:
            # Fallback robuste sans IA
            stock_bas = contexte["inventaire"]["stock_bas"]
            peremptions = contexte["inventaire"]["peremptions_7j"]
            repas = contexte["planning"]["repas_planifies_7j"]
            score = max(
                0,
                min(
                    100,
                    80 - (stock_bas * 3) - (peremptions * 2) + min(repas, 14),
                ),
            )
            parsed = ResumeHebdoIAResponse(
                resume=(
                    "Semaine globalement stable. "
                    f"{repas} repas planifies, {stock_bas} stocks bas, {peremptions} peremptions proches."
                ),
                points_attention=[
                    "Verifier les stocks faibles avant les courses",
                    "Prioriser les produits proches de peremption",
                ],
                recommandations=[
                    "Bloquer 20 minutes pour preparer la liste de courses",
                    "Planifier 1 activite famille exterieure selon la meteo",
                    "Revoir le budget de fin de mois avant weekend",
                ],
                score_semaine=int(score),
            )

        return {
            "periode": contexte.get("periode"),
            "contexte": contexte,
            "resume": parsed.resume,
            "points_attention": parsed.points_attention,
            "recommandations": parsed.recommandations,
            "SCORE_SEMAINE": parsed.score_semaine,
        }
    
    xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_1': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_1, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_2': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_2, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_3': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_3, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_4': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_4, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_5': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_5, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_6': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_6, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_7': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_7, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_8': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_8, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_9': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_9, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_10': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_10, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_11': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_11, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_12': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_12, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_13': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_13, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_14': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_14, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_15': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_15, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_16': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_16, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_17': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_17, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_18': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_18, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_19': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_19, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_20': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_20, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_21': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_21, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_22': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_22, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_23': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_23, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_24': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_24, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_25': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_25, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_26': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_26, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_27': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_27, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_28': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_28, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_29': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_29, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_30': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_30, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_31': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_31, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_32': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_32, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_33': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_33, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_34': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_34, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_35': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_35, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_36': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_36, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_37': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_37, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_38': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_38, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_39': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_39, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_40': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_40, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_41': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_41, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_42': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_42, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_43': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_43, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_44': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_44, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_45': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_45, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_46': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_46, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_47': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_47, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_48': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_48, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_49': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_49, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_50': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_50, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_51': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_51, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_52': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_52, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_53': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_53, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_54': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_54, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_55': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_55, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_56': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_56, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_57': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_57, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_58': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_58, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_59': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_59, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_60': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_60, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_61': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_61, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_62': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_62, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_63': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_63, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_64': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_64, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_65': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_65, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_66': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_66, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_67': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_67, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_68': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_68, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_69': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_69, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_70': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_70, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_71': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_71, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_72': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_72, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_73': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_73, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_74': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_74, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_75': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_75, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_76': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_76, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_77': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_77, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_78': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_78, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_79': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_79, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_80': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_80, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_81': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_81, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_82': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_82, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_83': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_83, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_84': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_84, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_85': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_85, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_86': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_86, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_87': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_87, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_88': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_88, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_89': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_89, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_90': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_90, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_91': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_91, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_92': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_92, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_93': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_93, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_94': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_94, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_95': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_95, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_96': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_96, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_97': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_97, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_98': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_98, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_99': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_99, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_100': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_100, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_101': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_101, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_102': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_102, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_103': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_103, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_104': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_104, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_105': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_105, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_106': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_106, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_107': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_107, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_108': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_108, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_109': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_109, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_110': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_110, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_111': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_111, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_112': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_112, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_113': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_113, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_114': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_114, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_115': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_115, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_116': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_116, 
        'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_117': xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_117
    }
    xǁResumeFamilleIAServiceǁgenerer_resume_hebdo__mutmut_orig.__name__ = 'xǁResumeFamilleIAServiceǁgenerer_resume_hebdo'


@service_factory("resume_famille_ia", tags={"dashboard", "ia", "famille"})
def obtenir_service_resume_famille_ia() -> ResumeFamilleIAService:
    """Factory singleton du service resume famille IA."""
    return ResumeFamilleIAService()


get_resume_famille_ia_service = obtenir_service_resume_famille_ia
